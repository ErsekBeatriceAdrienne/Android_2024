<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Transparent background -->
    <solid android:color="@android:color/transparent" />
    <!-- Rounded corners -->
    <corners android:radius="12dp" />
    <!-- Outline/border with color and thickness -->
    <stroke android:width="1dp" android:color="@color/unselected_icon_color" />
    <!-- Optional padding for inner text -->
    <padding android:left="8dp" android:right="8dp" android:top="4dp" android:bottom="4dp" />
</shape>